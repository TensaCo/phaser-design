# v3 — the current cut (2:59)

Requested changes and how v3 answers them:

| Note | v3 |
|---|---|
| Start with Mei-Lin in a dark studio lit only by a CRT; the studio lights up when she says what we built — one scene, not four | One continuous locked shot: Mei-Lin speaks in the dark by the glow of a CRT (a machine that literally fires electrons); on "…computes with **light**" the studio lights slam on (riser → impact, the score's first hit) and reveal PHASER beside her. The lights-on shot starts on the exact last frame of the dark take, so it plays as one shot. |
| Too many intermediate scenes in the hero section; the camera should fly through the product | One 30-second continuous fly-through rendered from the site's model: inside the cavity as the first pulse rises through the plates → out between the cells → over the knurled mirror knobs → the machine explodes apart as the darkroom becomes the white studio → labelled exploded view → reassembly → "PHASER". The modeled numbers ride over the move. |
| Diane's lip sync was wrong; the section should be customers reporting their experience, then the CEO explaining | Two customers (dramatized) in the white studio report what happened; then the CEO explains why, in physics terms. Diane is no longer in the film. |
| 2:10+ media too small; fly through the globe's power infrastructure; reveal the viewer's perspective | A pull-back from one machine to the planet: a 3D night Earth (NASA Black Marble — electricity made visible) dives from orbit onto North America and the East Coast grid, then aerial fly-throughs of a hyperscale datacenter campus, a continental transmission corridor and a power station. On screen: ~945 TWh by 2030, more than Japan (IEA projection). |
| Gallery much faster and more fluid | Ten shots at ~1.1 s with dissolves from the previous frame and a continuous push-in, under one line. |
| End slide too messy; iPhone brand-signature style | Black. One 650 nm beam draws in, collapses to a point, and the wordmark resolves from it; a single small URL. The disclosure card follows separately. |
| Too slow; no cinematic climax | 2:59 (from 3:32), cut to a new score with a hit on the lights, a driving hero theme, and a climax of rapid alternation (Cole to camera → product → Mei-Lin → 200–3,000× → exploded glimpse → globe → "We taught light to compute." → impact → silence). |
| Replace the CEO with a gritty Silicon Valley founder | Cole Mercer (TensaCo AI-agent persona), also now CEO on tensaco.ai; Diane Kowalski is President & COO there. |

Disclosures unchanged: the centre label on every frame; lower thirds "AI agent persona · dramatization · AI-generated" /
"dramatization · AI-generated"; the top-right AI tag on every generated shot; "modeled" on every PHASER figure; the
closing disclosure card.

Files: `src/edl_v3.py`, `src/script_v3.py`, `src/prompts_v3.py`, `src/prompts_v3_cast.py`, `src/lipsync_v3.py`,
`src/finalize_v3.sh`, `harness/globe.ts`, `src/renders.py` (`r3-fly`); docs `SHOT-LIST-v3.md`, `TRANSCRIPT-v3.md`,
`TRANSCRIPT-{meilin,cole,customer1,customer2}-v3.md`, `TYPOGRAPHY-v3.md`. Outputs `out/*-v3.mp4`, `out/stems-v3/`, `out/keyframes-v3/`.
